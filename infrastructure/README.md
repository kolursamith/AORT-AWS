# infrastructure/

**Owner: Owner B · cross-phase · Status: not started**

Shared deployment and environment scaffolding that is not specific to one
module — local orchestration helpers, environment definitions, and anything
supporting reproducible runs.

Note: the Phase 2 banking stack keeps its own `docker-compose.yml` inside
`banking/`, because it is that module's deployment.
