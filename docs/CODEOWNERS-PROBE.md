# THROWAWAY — CODEOWNERS control probe

Control for the probe in `banking/`. `/docs/` is owned by BOTH owners, so if
GitHub requests a reviewer here but not for `/banking/`, the difference is
caused by the sole `/banking/` owner also being the PR author.

**This PR will be closed without merging and this branch deleted.**
