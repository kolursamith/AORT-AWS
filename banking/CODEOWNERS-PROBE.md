# THROWAWAY — CODEOWNERS behaviour probe

This file exists only to test how the `main` ruleset's
`require_code_owner_review` behaves on a path owned by a single code owner
(`/banking/  @girinandanv9845-ship-it`).

**This PR will be closed without merging and this branch deleted.**
